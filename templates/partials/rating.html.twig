<span class="rating mt-4">
    <p class="">Note globale</p>
    {% for i in 1..5 %}
        {% if i <= rate %}
            <i class="fas fa-star"></i>
                           {% else %}
            <i class="far fa-star"></i>
        {% endif %}
    {% endfor %}
    <br>
    <small> Moyenne calculée sur {{ product.product.avis|length }} avis !</small>
</span>