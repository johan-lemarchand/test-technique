{% extends 'base.html.twig' %}

{% block title %}ajout de votre commentaire{% endblock %}

{% block body %}
    {% include'product/header.html.twig' %}

    <div class="container">
        <div class="row d-flex justify-content-center mt-5">
            <div class="col-sm-4">
                <h2 class=page-header>Ajouter un commentaire</h2>

                {{form_start(form)}}
                {{form_widget(form)}}
                <button type="submit" class="btn btn-primary">soumettre</button>
                {{form_end(form)}}

            </div>
        </div>
    </div>

{% endblock %}
